meta {
  name: login
  type: http
  seq: 4
}

post {
  url: http://localhost:5000/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "login": "jalel",
    "password": "123456"
  }
  
}

settings {
  encodeUrl: true
  timeout: 0
}
